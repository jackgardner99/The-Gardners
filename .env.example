# Copy this file to .env.local and fill in real values.
# All four are safe to expose client-side (read-only, public-scoped),
# but should still be referrer/domain restricted where the provider allows it.

VITE_GOOGLE_CALENDAR_ID=
VITE_GOOGLE_CALENDAR_API_KEY=
VITE_SHOPIFY_DOMAIN=
VITE_SHOPIFY_STOREFRONT_TOKEN=
